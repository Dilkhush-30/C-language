// First C Program

#include <stdio.h>  // Header file

int main() {        // Main function, where execution begins
    printf("Hello, World!");  // Print statement (ends with a semicolon)
    return 0;       // Return statement to indicate program success
}                    // Braces enclose the block of code